#!/usr/bin/env bash
##################################
# Keep max dirty pages to 32MB
##################################

if [ -z "$(df --output=source '/' | grep $1)" ]; then
    echo 1 > /sys/block/$1/bdi/strict_limit
    echo 33554432 > /sys/block/$1/bdi/max_bytes
fi
